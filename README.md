env file for this project
============================
DATABASE_NAME=future-profile
DATABASE_USER=postgres
DATABASE_PASSWORD=root
DATABASE_DIALECT=postgres
DATABASE_HOST=localhost
DATABASE_PORT=5432

After add env flie create vertual evironemt file to download the required libraries to install 
firt cactivate the  vertual evironemt with ur respected name
python -m venv {environment_name}  example: python -m venv fast-venv

fast-venv\srcipts\activate

then run below command to install the libraries

pip install requirment.txt
